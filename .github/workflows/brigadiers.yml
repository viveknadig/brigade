name: Brigadiers

# Keep the "🦁 Brigadiers" contributor wall in README.md fresh. Runs on every
# merge to main (so a new contributor shows up right after their PR lands) plus a
# weekly sweep (catches contributions that arrive without a push) and on demand.
# The generator commits ONLY when the wall actually changes, so most runs are no-ops.

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1" # Mondays 06:00 UTC
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: brigadiers
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    # Don't react to the bot's own wall commit (a GITHUB_TOKEN push doesn't
    # re-trigger workflows anyway — this is belt-and-suspenders).
    if: github.actor != 'github-actions[bot]'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Regenerate the Brigadiers wall
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/update-brigadiers.mjs
      - name: Commit if the wall changed
        run: |
          if [[ -n "$(git status --porcelain README.md)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add README.md
            git commit -m "docs: refresh Brigadiers contributor wall [skip ci]"
            git push
          else
            echo "Brigadiers wall unchanged — nothing to commit."
          fi
