# A dev-only tool (madge) declares an older TypeScript peer range than the repo
# pins. The conflict is cosmetic — nothing at runtime depends on the resolution —
# but it makes a strict `npm install` / `npm ci` fail on a clean machine (incl.
# CI). Relax peer resolution so installs are reproducible.
legacy-peer-deps=true
